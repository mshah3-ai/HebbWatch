# Plugin System

The integrations folder is reserved for model hooks. The first integration is a lightweight PyTorch helper that can watch Linear layers through forward hooks.

Long-term goal: stream real model weights into the same observability and rendering pipeline.
