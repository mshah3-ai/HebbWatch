# Rendering Engine

The terminal renderer uses Rich live displays. Weight matrices are downsampled to fit the terminal and rendered as block characters.

Future improvements can add richer ANSI color gradients, keyboard controls, and GIF export.
